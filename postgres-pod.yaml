apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    name: postgres-pod
    app: demo-app
    
spec:
    containers:
    - name: postgres
      image: postgres:9.4
      environment:
        POSTGRES_PASSWORD: root
      ports:
      - containerPort: 5432
        